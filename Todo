Read about:
    Chunked transfer encoding
    Mandatory headers
    Authentification
	Transfer-Encoding
    
headers:
    Content-Encoding
    Last-Modified
	Server
    Last-Modified
	

loop throught Servers
    run server
        
        listen in all given ports [Mandatory]
        
        set server_name when sending response
        create a new location with serverProps

        check if the given url matches any location
            loop throught locations
                if location matches
                    check if there is a http redirection
                        => redirect
                    otherwise overrideLocationProps
                        override server error pages if any [404 index.html ....]
                        override upload path
                        override root [root /www/var/html]
                        override autoindex
                        override cgi
                        override client_max_body_size

        otherwise check if there is a http redirection and the url doesnt matches any location
            => redirect
        check if the request body size exceed client_max_body_size
        check if there is a limit_except
            loop throught the allowed methods
            if the method not matches
                ERROR : [METHOD NOT ALLOWED PAGE]
        loop throught all cgis
        check if the given file has a cgi
            => if so then pass it to cgi
        otherwise read it 

            

1 - nsbn kwach
2 - sac de cochaeg => khalid
3 - chkara
4 - tabla => omar
5 - 9lawya => bone
    - bota
    - brad
    - botaji